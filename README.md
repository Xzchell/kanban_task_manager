# Real-Time Kanban Task Manager SaaS

Веб-приложение для управления задачами и координации команд в режиме реального времени.

Проект реализован как MVP с разделённой архитектурой (frontend / backend / real-time слой) и ориентирован на сценарии с высокой динамикой изменений (хакатоны, короткие спринты), где важна синхронизация состояния между пользователями без перезагрузки страницы.


### Принципы
- разделение ответственности между слоями
- минимизация логики на frontend
- перенос части агрегации данных на уровень БД
- выделенный real-time слой
---

## 🚀 Технологический стек

* **Frontend:** React, TypeScript, React Context + Custom Hooks (`useAuth`, `useBoard`), React Router (`useNavigate`).


* **Backend API:** Чистый PHP (ООП, PDO), PHPMailer (SMTP-уведомления).


* **СУБД:** MySQL 8.0+ (с глубокой интеграцией транзакционной бизнес-логики и JSON-агрегации на уровне хранимых процедур и функций).


* **Real-Time Слой:** Node.js, Socket.io (WebSocket-сервер для мгновенного broadcasting событий).

---

## 🛠 Ключевые особенности

### 1. Агрегация данных на уровне БД
Используются хранимые функции (например, `GetBoardDetailsFunction`), возвращающие вложенные JSON-структуры.

Преимущества:
- уменьшение количества запросов
- снижение нагрузки на backend
- сокращение постобработки

---

### 2. Управление сессиями
Авторизация реализована через токены, хранящиеся в HttpOnly cookies.

Реализовано:
- защита от доступа через JavaScript (XSS mitigation)
- механизм завершения активных сессий

Для production предусмотрено:
- переход на HTTPS
- использование Secure cookies

---

### 3. Частичное обновление данных
Обновление связанных сущностей (теги, исполнители) реализовано без полной перезаписи.

Плюсы:
- снижение нагрузки на БД
- уменьшение сетевого трафика

---

### 4. Логическая изоляция данных (Multi-tenancy)
Разделение данных рабочих пространств на уровне backend.

Обеспечивает:
- разграничение доступа
- безопасность данных

---

### 5. Real-time синхронизация
Отдельный WebSocket-сервер обрабатывает события системы.

Поддерживается:
- обновление задач без перезагрузки
- синхронизация состояния между клиентами

---

### 6. UI
Два режима интерфейса:
- визуальный (glassmorphism)
- упрощённый (для слабых устройств)

---

## 👥 Ролевая модель

Контроль доступа реализован через `BoardPermissions`.

- **Owner** — полный доступ
- **User** — работа с задачами
- **Spectator** — только просмотр

---

## ⚙️ Ограничения MVP

- отсутствует HTTPS (используется в production-сценарии)
- отсутствует централизованное логирование
- базовая обработка ошибок

---

## 💻 Локальное развертывание (Инструкция)

### 📋 Требования к окружению

Перед началом установки убедись, что у тебя развернуты:

* Веб-сервер (Apache/Nginx) с поддержкой **PHP 8.0+**
* **MySQL Server 8.0+**
* **Node.js v16+** & npm

---

### 1. Настройка базы данных (MySQL)

1. Создай новую базу данных в твоей СУБД:
```sql
CREATE DATABASE manager_tasks_bd CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

```


2. Импортируй структуру таблиц (14 связанных таблиц) из дампа проекта.


3. **Важно:** Обязательно импортируй все хранимые процедуры и функции (такие как `GetBoardDetailsFunction`, `CreateBoardFunction`, `add_task`), так как на них завязана критическая часть бизнес-логики приложения.



### 2. Развертывание серверной части (Backend API & WebSockets)

#### Настройка PHP API:

1. Перенеси файлы бэкенда в рабочую директорию твоего веб-сервера.
2. В конфигурационном файле подключения к БД (обычно `db.php` или аналогичном модуле внутри слоя моделей/контроллеров) укажи актуальные доступы:


```php
// Пример параметров конфигурации PDO
$host = 'localhost';
$db   = 'manager_tasks_bd';
$user = 'твой_пользователь';
$pass = 'твой_пароль';

```


3. Для работы модуля почтовых уведомлений укажи валидные данные SMTP-сервера (в коде инициализации PHPMailer).



#### Настройка WebSocket-сервера:

1. Перейди в директорию сокет-сервера (модуль на Node.js):


```bash
cd path/to/backend/websocket-server

```


2. Установи зависимости:
```bash
npm install

```


3. Запусти WebSocket-сервер (по умолчанию работает на порту `3001`):


```bash
node index.js

```



### 3. Развертывание клиентской части (Frontend)

1. Перейди в корневую директорию фронтенд-приложения:


```bash
cd path/to/frontend

```


2. Установи необходимые npm-пакеты:
```bash
npm install

```


3. Настрой URL-адреса для взаимодействия с API и WebSockets в конфигурационных файлах фронтенда (укажи локальный адрес твоего бэкенд API и сокет-сервера `http://localhost:3001`).


4. Запусти приложение в режиме разработки:
```bash
npm start

```


5. Открой в браузере [http://localhost:3000](http://localhost:3000).

---

## 🛠 Демонстрационный доступ

Если вы хотите протестировать функционал платформы на развернутом инстансе без прохождения этапа верификации почты:

* **Тестовый URL:** [http://194.226.20.174/](https://www.google.com/search?q=http://194.226.20.174/)

* **Логин (Email):** `testboard62@gmail.com`

* **Пароль:** `testqwerty1234`


---

**Автор проекта:** Большаков Владислав Сергеевич (2026).
